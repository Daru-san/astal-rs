[options]
library = "AstalNetwork"
version = "0.1"
target_path = "."
girs_directories = ["../../gtk-girs/", "../../girs/"]
work_mode = "sys"
single_version_file = true
min_cfg_version = "3.4"

external_libraries = ["GLib", "GObject", "Gtk4", "Gio"]
